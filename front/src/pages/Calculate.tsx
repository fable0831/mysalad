import Header from "../components/header/Header"

type Props = {}

const Calculate = (props: Props) => {
  return (
    <div>
      <Header />
      Calculate
      </div>
  )
}

export default Calculate